<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>フォローしている人たち</h1><br>
    <% @follower.each do |follower| %>
    <% @post = follower.posts.all %>
      <%= render "posts/posts_index", posts: @post %>
    <% end %>
  </div>
</div>
