<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <% @user.followed_user.each do |followed| %>
      <%= attachment_image_tag followed, :profile_image, fallback: "mountain.jpg" %>
      <%= link_to followed.name, user_path(followed) %>
    <% end %>
  </div>
</div>
